# Web framework
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-multipart==0.0.27
python-dotenv==1.2.2

# AI coaching (Groq — https://console.groq.com/keys)
groq==1.4.0

# Audio: transcription (also requires ffmpeg on PATH)
faster-whisper==1.2.1

# Visual: gaze / eye-contact detection.
# mediapipe hard-requires opencv-contrib-python and numpy<2, so don't swap in
# opencv-python-headless — its recent builds want numpy>=2 and the resolve fails.
mediapipe==0.10.21
opencv-contrib-python==4.11.0.86
numpy==1.26.4
